# Core RAG — embedding + retrieval (still needed)
faiss-cpu>=1.7.4
rank-bm25>=0.2.2
sentence-transformers>=2.7.0    # MiniLM embedding + reranker

# PDF processing
pymupdf>=1.23.0                 # fitz — fast PDF text extraction

# HTML → Image rendering
playwright>=1.43.0              # after pip install, run: playwright install chromium

# UI
gradio>=4.29.0

# Utilities
numpy>=1.26.0
Pillow>=10.3.0
huggingface-hub>=0.22.0
tqdm>=4.66.0

# NOTE: torch/transformers/accelerate are no longer needed.
# The LLM (phi3:mini) runs via Ollama — install from https://ollama.com/download
# Then run once: ollama pull phi3:mini
